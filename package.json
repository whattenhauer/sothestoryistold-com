{
  "name": "sothestoryistold-com",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.0.0",
    "vite": "^6.0.0",
    "wrangler": "^4.112.0"
  }
}
